<UserControl xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Arpeggio.Daw.Views.AnalysisView" Background="{StaticResource Arpeggio.Panel}">
  <Grid RowDefinitions="Auto,Auto,*" Margin="12">
    <ComboBox x:Name="TrackSelector" HorizontalAlignment="Stretch" Margin="0,0,0,8" />
    <Button x:Name="AnalyzeButton" ContentTemplate="{StaticResource Arpeggio.Content.Analyze}" Grid.Row="1" Content="解析" HorizontalAlignment="Stretch" Margin="0,0,0,8" ToolTip.Tip="{ReflectionBinding Content, RelativeSource={RelativeSource Self}}" />
    <TextBox x:Name="AnalysisText" Grid.Row="2" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
             Text="解析ボタンで音量・周波数・警告を確認できます。" />
  </Grid>
</UserControl>
