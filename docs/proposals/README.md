# proposals

Codex に「次に何を作るべきか」を提案させた記録（`codex-propose` skill）。

- 1 ファイル 1 ラン。ファイル名は提案ランを回した日付（`YYYY-MM-DD.md`）
- **判定を書いてからコミットする。** 採用 / 保留 / 却下と理由を案ごとに残す
- 採用した案は `docs/design.md`（または派生の設計書）へ書き写してから実装する。**提案書は正本ではない**
